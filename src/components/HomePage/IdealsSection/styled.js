import styled from 'styled-components'

// Colors
import * as color from '../../colors'

export const Item = styled.li`
    padding: 10px;
    margin: 10px 0;
    border-left: 10px solid ${color.orange_2};
`;